[project]
name = "robocowork"
version = "0.1.0"
description = "The MCP server for RoboCo-nt"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
    "questionary>=2.1.1"
]